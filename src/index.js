import React from 'react';
import ReactDOM from 'react-dom/client';
import EducationPortal from './EducationPortal';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <EducationPortal />
  </React.StrictMode>
);
